{
  "name": "note-collab-app",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "pnpm --filter next-app dev",
    "dev:socket": "pnpm --filter socket-server start",
    "build": "pnpm --filter next-app build",
    "start": "pnpm --filter next-app start",
    "lint": "eslint"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
